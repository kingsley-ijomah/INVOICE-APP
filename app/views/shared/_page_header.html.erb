<div class="pageheader">
  <h1 id="crums-controller"><%= bread_crums_controller %></h1>
  <div class="breadcrumb-wrapper hidden-xs">
    <span class="label">You are here:</span>
    <ol class="breadcrumb">
      <% build_bread_crums.each do |crumb| %> 
        <li><%= crumb %></li>
      <% end %>
    </ol>
  </div>
</div>
